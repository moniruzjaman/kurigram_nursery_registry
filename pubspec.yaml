name: kurigram_nursery_registry
description: Zero-API-Key Nursery GIS for Kurigram
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_map: ^6.1.0
  latlong2: ^0.9.0
  sqflite: ^2.3.0
  path_provider: ^2.1.1
  flutter_image_compress: ^2.1.0
  image_picker: ^1.0.4
  geolocator: ^10.1.0
  url_launcher: ^6.2.5
  share_plus: ^7.2.1
  path: ^1.8.3
  intl: ^0.18.1

flutter:
  uses-material-design: true
